kcc => kk's C language Compiler
2013-1-3:
		commit:重新处理typedef，改一条文法，使处理类似|typedef struct Str { ... } Node;|更简单
		下一步处理 # include, #  define, #if, # else, #endif #ifdef  #ifndef if (a && b)  if (a || b) 等
